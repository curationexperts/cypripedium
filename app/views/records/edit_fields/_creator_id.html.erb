<%= f.input key,
        as: :multi_value,
        placeholder: 'Autocomplete field.',
        input_html: {
          class: 'form-control',
          id: 'creator_id_autocomplete',
          data: { 'autocomplete-url' => "/authorities/search/creator_authority",
                  'autocomplete' => key}
        },

        required: f.object.required?(key) %>
