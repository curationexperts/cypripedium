<%= f.input key,
        as: :multi_value,
        placeholder: 'Autocomplete field.',
        input_html: {
          class: 'form-control',
          data: { 'autocomplete-url' => "/authorities/search/local_names/creators",
                  'autocomplete' => key,
                 'field-type' => 'linked_data' }
        },

        required: f.object.required?(key) %>
