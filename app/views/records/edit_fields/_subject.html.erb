<%= f.input key,
        as: :multi_value,
        placeholder: 'Autocomplete field. Enter a JEL code',
        input_html: {
          class: 'form-control',
          data: { 'autocomplete-url' => "/qa/search/local/jels",
                  'autocomplete' => key,
                 'field-type' => 'linked_data' }
        },

        required: f.object.required?(key) %>
